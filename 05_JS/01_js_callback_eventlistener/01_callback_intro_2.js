// JS에서는 아래의 3가지 조건ㅇ르 만족하는 것을 1급 객체(first class object)라고 한다
// 1. 변수에 담을 수 있다.
// 2. 인자로 전달할 수 있다.
// 3. 반환값(return)으로 전달할 수 있다.

/*
const fco = function(){
  return n => n + 1
}
console.log(fco)

// 도전과제 -> nnum_101 변수에 101을 담아야 한다.
const num_101 = fco()(100)
console.log(num_101)
*/